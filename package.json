{
  "name": "kensai-quiz-core-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=18"
  },
  "description": "Workspace root for the Kensai Quiz ecosystem (spec + headless core; a player will follow).",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "sync:schema": "pnpm -r run sync:schema",
    "demo": "pnpm -r run build && pnpm -C packages/player-web serve"
  }
}
